---
component_skel:
  designate:
    belongs_to:
      - designate_all

container_skel:
  designate_container:
    belongs_to:
      - shared-infra_containers
    contains:
      - designate
    properties:
      service_name: designate

